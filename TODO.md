Need to migrate from main:

Scheduler and TensorBoard
Resume-safe checkpoints
Dataset/schema compatibility validation
DataLoader worker pipeline
Generic rollout evaluation
Hybrid physics projection
Contact-rich dataset generation
Full training workflow